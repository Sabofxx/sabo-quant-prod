"""
data/ — ingestion, cleaning, streaming of OHLCV candles.

Implementations of ``DataProvider`` (see ``sabo_lit.core.interfaces``). May
import from ``sabo_lit.core`` only. Must NOT import from any other production
module. Must NOT import from ``research/``, ``sandbox/`` or ``notebooks/``.
"""
