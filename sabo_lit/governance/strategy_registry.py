"""
Strategy registry — implements ``ComponentRegistry`` for ``SubStrategy``.

Each registered sub-strategy carries a ``ComponentManifest`` and a
``ValidationReport``. Only registered sub-strategies may be enabled in
``config.meta_strategy.enabled_sub_strategies``.

Phase 0: placeholder. Implementation deferred to Phase 7.
"""
