"""
Complexity metrics — implements ``ComplexityMonitor``.

Produces ``ComplexityReport`` snapshots: active features, active models,
active sub-strategies, max dependency depth, total LOC. Pure sensor — does
NOT take trading decisions and does NOT mutate any registry.

The dashboard surfaces these counts so reviewers can see the system's
complexity budget at a glance.

Phase 0: placeholder. Implementation deferred to Phase 7.
"""
