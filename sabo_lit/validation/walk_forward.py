"""
Walk-forward analysis — in-sample / out-of-sample rolling folds.

Implements ``ValidationSuite``. Produces ``WalkForwardResult`` as part of
the aggregate ``ValidationReport``.

Phase 0: placeholder. Implementation deferred to Phase 6.
"""
